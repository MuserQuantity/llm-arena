"use client";

import React from "react";

// This component is now replaced by the inline TaskDrawer in the tasks page.
// Keeping this file as an empty export for backward compatibility.
export function TaskForm() {
  return (
    <div className="text-muted-foreground text-center py-10">
      Task form has been moved to the Tasks page.
    </div>
  );
}
